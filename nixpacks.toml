[phases.setup]
nixPkgs = ["nodejs", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci", "cd server && npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd server && node index.cjs"
