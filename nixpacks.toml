[phases.setup]
nixPkgs = ["nodejs", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["cd server && npm install"]

[start]
cmd = "cd server && node index.cjs"
