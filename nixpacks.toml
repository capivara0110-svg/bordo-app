[phases.setup]
nixPkgs = ["nodejs", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install", "cd server && npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd server && node index.cjs"
